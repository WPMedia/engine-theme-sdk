import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { number, select, text } from "@storybook/addon-knobs";

import Overline from '../../src/elements/Overline'

<Meta title="Elements/Overline" component={Overline} />

# Overline


## ArgsTable

<ArgsTable of={Overline} />

## Stories

export const content = {
  websites: {
    'story-book': {
      website_section: {
        _id: text('websites[arcSite].website_section._id', '/news/'),
        name: text('websites[arcSite].website_section.name', 'News'),
      },
    },
  },
};

<Canvas>
  <Story name="Story Object">
    <Overline story={content} />
  </Story>
</Canvas>

export const sponsoredStory = {
  owner: {
    sponsored: true,
  },
};

<Canvas>
  <Story name="Sponsored Story Object">
    <Overline story={sponsoredStory} />
  </Story>
</Canvas>

export const sponsoredStoryOverride = {
  label: {
    basic: 'Overriding sponsored text',
  },
  owner: {
    sponsored: true,
  },
};

<Canvas>
  <Story name="Sponsored Story Object Label Override">
    <Overline story={sponsoredStoryOverride} />
  </Story>
</Canvas>



<Canvas>
  <Story name="Custom Text and URL">
    <Overline customText="Overline Text" customUrl="http://arcxp.com" />
  </Story>
</Canvas>
